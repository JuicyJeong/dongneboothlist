# AGENTS.md

## Agent Defaults: Context7 & Sequential Thinking

이 저장소에서 에이전트는 별도의 지시가 없더라도 최신 문서/예제를 확보하고 단계적 사고를 적용해야 한다.

### Default Behavior

모든 작업(기획, 구현, 테스트, 리팩터링, 문서화) 시작 시 다음 지시가 항상 기본으로 적용된다고 간주한다.

- "Use Context7"
- "Use Sequential Thinking"

별도의 프롬프트에 위 문구가 없더라도, 에이전트는 이를 자동으로 적용하여 도구 호출과 추론 체인을 구성한다.

모델 한계나 맥락 길이 문제로 지시가 누락될 가능성이 있을 때, 우선순위는 다음과 같다.

1. Context7로 최신 공식 문서/코드 예제를 선확보
2. Sequential Thinking으로 단계 계획 수립 및 필요 시 추가 반성 단계 수행

### 언어 및 문서 규칙

- 대답은 항상 한글로 대답하며, 문서 작성 시 명사 또는 코드 어휘 등과 같은 특수한 내용을 제외하고는 한글로 작성한다.
- 모든 md 파일은 `docs` 폴더에 저장한다.

### 변경 이력 관리

코드 수정이 되거나 변경 사항 발생 시 항상 날짜 시간을 기입하여 `Work_History.md`에 기록한다.

- 날짜 시간 형식은 `YYYY-MM-DD HH:MM (KST)`로 통일한다.
- 기록 형식 예시:

```
2026-04-11 16:30 (KST)

변경 파일: 경로/파일명
변경 내용: 구체적인 변경 사항
사유: 변경 이유
```

### 구조 문서화

폴더 구조, 폴더별 역할, 코드 분석, 프로세스 흐름 등과 같은 내용은 `Structure.md`에 기록한다.

폴더 구조 또는 코드 구성이 변경될 때마다 `Structure.md`를 함께 업데이트한다.

### 임시 테스트

간이 테스트 등을 진행할 때에는 `temp` 폴더를 만들고 해당 폴더에서 작업한다.

테스트 완료 후 `temp` 폴더는 삭제하거나, 유지가 필요한 경우 그 이유를 `Work_History.md`에 기록한다.
